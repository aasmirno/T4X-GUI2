// mesh fragment shader
#version 420 core

uniform sampler2D image_texture;

in vec2 color_coord;
in float height;
out vec4 frag_color;

void main()
{
    frag_color = texture(image_texture, color_coord);
}