//testing fragment shader
#version 420 core

uniform float ambient_level = 0.1;

out vec4 frag_color;


void main()
{
    vec4 ambient = ambient_level * vec4(1.0, 1.0, 1.0, 1.0 / ambient_level);
    frag_color = ambient * vec4(1.0, 0.5, 0.3, 1.0);
}
